<!-- 改编自：研究装修艺术主题项目提示词/ld-427/ld-427.md；后端由 Django + Python（DRF）改造为 Go 1.22 + Gin + GORM -->

## 项目主要需求

### 项目概述

【纯后端服务】装修预算与支出管理 API 服务。面向装修公司和业主提供预算编制、支出审批、供应商管理和对账结算的 RESTful API。**本项目用于验证模型跨文件协同改动能力，禁止将多个职责合并进单一文件。**

### 核心实体（≥ 3 个，每个贯穿后端全链路）

1. **预算表（BudgetSheet）** — 字段：所属项目ID、预算名称、总金额、已支出金额、冻结金额（审批中的支出）、可用余额（自动计算）、状态（Draft/Active/Locked/Archived）、创建人ID、审批人ID、版本号
   - 关联：1个预算表 → 多个预算项
   - **贯穿后端**

2. **预算项（BudgetItem）** — 字段：所属预算表ID、类别（Design/Material/Labor/Furniture/Appliance/Contingency/Other）、子类名称、预算金额、已支出金额、差异金额、排序、备注
   - **贯穿后端**

3. **支出记录（ExpenseRecord）** — 字段：所属预算项ID、支出金额、支出日期、支出方式（Cash/BankTransfer/Credit/Company）、供应商ID、发票号、支出描述、附件URL、审批状态（Draft/Submitted/Approved/Rejected/Paid）、申请人ID、审批人ID、审批意见、付款日期
   - **贯穿后端**

4. **供应商（Supplier）** — 字段：供应商名称、类别（Material/Furniture/Appliance/Labor/Design/Other）、联系人、联系电话、地址、开户行、银行账号、合作状态（Active/Suspended/Blacklisted）、评分（1-5）
   - **贯穿后端**

5. **对账单（Reconciliation）** — 字段：所属项目ID、对账期间（月份）、供应商ID、应付金额、已付金额、未付金额、对账状态（Pending/Confirmed/Disputed/Resolved）、确认人ID
   - **贯穿后端**

### 横切关注点（≥ 2 个）

1. **RBAC 权限控制**
   - 角色：Admin / FinanceManager / ProjectManager / Accountant / Owner
   - 触达文件层：DB role 表 + `middleware/rbac.go` + `handler` 层权限检查 + `model`/`dto` 类型定义
   - 特殊规则：Owner 只能查看不能操作，Accountant 可以录入支出但不能审批

2. **审计日志**
   - 记录预算调整、支出审批、付款确认、对账操作
   - 触达文件层：DB audit_log 表 + `middleware/audit_log.go` + `service` 层埋点 + `model` 类型定义

3. **接口限流**
   - 触达文件层：`middleware/rate_limit.go` + Redis

### 共享枚举/常量（≥ 2 组）

1. **预算状态 BudgetStatus**：Draft / Active / Locked / Archived
   - 定义位置：`backend/internal/constants/enums.go`

2. **支出审批状态 ExpenseStatus**：Draft / Submitted / Approved / Rejected / Paid

3. **支出方式 PaymentMethod**：Cash / BankTransfer / Credit / Company

4. **供应商状态 SupplierStatus**：Active / Suspended / Blacklisted

5. **预算类别 BudgetCategory**：Design / Material / Labor / Furniture / Appliance / Contingency / Other

### 后端中间件清单（≥ 2 个）

1. **`JWTAuthMiddleware`** — JWT 鉴权（`github.com/golang-jwt/jwt/v5`）
2. **`RBACMiddleware`** — 角色权限校验
3. **`AuditLogMiddleware`** — 审计日志
4. **`ErrorHandlerMiddleware`** — 统一异常
5. **`RateLimitMiddleware`** — 接口限流
6. **`RequestLoggerMiddleware`** — 请求日志
7. **`ValidationMiddleware`** — 参数校验

### 技术要求

- **英文短名（用于 Docker Compose）**：`renovation-budget-api`
- **后端端口**：Docker 对外映射 19306；本地开发默认监听 3000（Docker 内部统一 8080）
- **后端**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL 15（使用 `gorm.io/driver/postgres`）
- **缓存**：Redis 7（限流 + 预算快照缓存，使用 `github.com/redis/go-redis/v9`）
- **认证**：JWT，使用 `github.com/golang-jwt/jwt/v5`，保留 RBAC 角色描述
- **API文档**：Swagger，使用 `github.com/swaggo/gin-swagger` + `github.com/swaggo/files`，通过 `swag init -g cmd/server/main.go -o docs` 生成，访问地址 `/swagger/index.html`

### 文件结构强制清单

**后端（严禁合并职责到单一文件）**：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go                # 程序入口：加载配置、初始化 DB/Redis、注册路由、启动 HTTP 服务
├── docs/                          # Swagger 生成的 API 文档
└── internal/
    ├── config/                    # 配置结构体与加载逻辑
    ├── model/                     # GORM 模型：budget_sheet.go, budget_item.go, expense_record.go, supplier.go, reconciliation.go, user.go, role.go, audit_log.go
    ├── repository/                # 数据访问层：budget_repository.go, item_repository.go, expense_repository.go, supplier_repository.go, reconciliation_repository.go
    ├── service/                   # 业务逻辑层：budget_service.go, expense_service.go, supplier_service.go, reconciliation_service.go, balance_calculator.go
    ├── handler/                   # Gin HTTP 处理层：budget_handler.go, item_handler.go, expense_handler.go, supplier_handler.go, reconciliation_handler.go
    ├── router/                    # router.go：集中定义路由分组和中间件挂载
    ├── middleware/                # auth.go, rbac.go, audit_log.go, request_logger.go, error_handler.go, rate_limit.go, validation.go
    ├── dto/                       # 请求/响应结构体与参数校验标签：budget_dto.go, item_dto.go, expense_dto.go, supplier_dto.go, reconciliation_dto.go，以及 budget_filter.go, expense_filter.go
    └── constants/                 # enums.go：预算状态、支出审批状态、支出方式、供应商状态、预算类别
```

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


### 部署要求

后端 + PostgreSQL 15 + Redis 7，Docker Compose 编排。
- 顶层 `name: renovation-budget-api`
- 不写 `version:` 字段
- backend 服务内部端口统一 `8080`，对外映射 `${BACKEND_PORT:-19306}:8080`
- PostgreSQL + Redis 命名卷 + PostgreSQL healthcheck
- 后端 `depends_on` 等待 PostgreSQL 和 Redis 就绪
- `.env` 必含：`COMPOSE_PROJECT_NAME`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_PORT`, `JWT_SECRET`, `REDIS_URL`, `BACKEND_PORT`

backend/Dockerfile 必须使用 Go 多阶段构建：
```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

## 构建验证清单（硬性约束）

- 后端必须能通过 `go mod tidy` 生成完整 `go.mod`/`go.sum`，依赖声明包括 `github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/golang-jwt/jwt/v5`、`github.com/redis/go-redis/v9`、`github.com/swaggo/gin-swagger`、`github.com/swaggo/files` 等
- 后端必须能通过 `go build ./...`；本地运行入口为 `go run ./cmd/server`
- 每个实体必须贯穿 model、repository、service、handler、dto，禁止将多个实体的职责合并进单一文件
- 不得使用 `version:` 字段，Compose V2 顶层声明 `name: renovation-budget-api`

## Git 要求

在项目根目录执行 `git init`，并创建 `.gitignore`，至少包含：
```
.env
.DS_Store
*.log
bin/
```

## README 写法

中文 README，Docker 启动最前（`cp .env.example .env` + `docker compose up -d`）、Swagger 地址（`http://localhost:19306/swagger/index.html`）、技术栈表格（后端写 Go 1.22 + Gin + GORM）、目录结构、枚举位置、License。
